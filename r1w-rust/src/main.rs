use std::io::prelude::*;

fn main() {
    // Bufwriter
    let out = std::io::BufWriter::new(std::io::stdout().lock());

    // Image
    let image_width:  i32 = 256;
    let image_height: i32 = 256;

    // Render

}
